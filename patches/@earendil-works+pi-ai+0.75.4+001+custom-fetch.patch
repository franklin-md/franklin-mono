diff --git a/node_modules/@earendil-works/pi-ai/dist/providers/anthropic.js b/node_modules/@earendil-works/pi-ai/dist/providers/anthropic.js
index 7406574..f56d0de 100644
--- a/node_modules/@earendil-works/pi-ai/dist/providers/anthropic.js
+++ b/node_modules/@earendil-works/pi-ai/dist/providers/anthropic.js
@@ -317,7 +317,7 @@ export const streamAnthropic = (model, context, options) => {
                 }
                 const cacheRetention = options?.cacheRetention ?? resolveCacheRetention();
                 const cacheSessionId = cacheRetention === "none" ? undefined : options?.sessionId;
-                const created = createClient(model, apiKey, options?.interleavedThinking ?? true, shouldUseFineGrainedToolStreamingBeta(model, context), options?.headers, copilotDynamicHeaders, cacheSessionId);
+                const created = createClient(model, apiKey, options?.interleavedThinking ?? true, shouldUseFineGrainedToolStreamingBeta(model, context), options?.headers, copilotDynamicHeaders, cacheSessionId, options?.fetch);
                 client = created.client;
                 isOAuth = created.isOAuthToken;
             }
@@ -591,7 +591,7 @@ export const streamSimpleAnthropic = (model, context, options) => {
 function isOAuthToken(apiKey) {
     return apiKey.includes("sk-ant-oat");
 }
-function createClient(model, apiKey, interleavedThinking, useFineGrainedToolStreamingBeta, optionsHeaders, dynamicHeaders, sessionId) {
+function createClient(model, apiKey, interleavedThinking, useFineGrainedToolStreamingBeta, optionsHeaders, dynamicHeaders, sessionId, fetch) {
     // Adaptive thinking models (Opus 4.6, Sonnet 4.6) have interleaved thinking built-in.
     // The beta header is deprecated on Opus 4.6 and redundant on Sonnet 4.6, so skip it.
     const needsInterleavedBeta = interleavedThinking && !supportsAdaptiveThinking(model.id);
@@ -607,6 +607,7 @@ function createClient(model, apiKey, interleavedThinking, useFineGrainedToolStre
             apiKey: null,
             authToken: null,
             baseURL: resolveCloudflareBaseUrl(model),
+            fetch,
             dangerouslyAllowBrowser: true,
             defaultHeaders: mergeHeaders({
                 accept: "application/json",
@@ -625,6 +626,7 @@ function createClient(model, apiKey, interleavedThinking, useFineGrainedToolStre
             apiKey: null,
             authToken: apiKey,
             baseURL: model.baseUrl,
+            fetch,
             dangerouslyAllowBrowser: true,
             defaultHeaders: mergeHeaders({
                 accept: "application/json",
@@ -640,6 +642,7 @@ function createClient(model, apiKey, interleavedThinking, useFineGrainedToolStre
             apiKey: null,
             authToken: apiKey,
             baseURL: model.baseUrl,
+            fetch,
             dangerouslyAllowBrowser: true,
             defaultHeaders: mergeHeaders({
                 accept: "application/json",
@@ -657,6 +660,7 @@ function createClient(model, apiKey, interleavedThinking, useFineGrainedToolStre
         apiKey,
         authToken: null,
         baseURL: model.baseUrl,
+        fetch,
         dangerouslyAllowBrowser: true,
         defaultHeaders: mergeHeaders({
             accept: "application/json",
diff --git a/node_modules/@earendil-works/pi-ai/dist/providers/azure-openai-responses.js b/node_modules/@earendil-works/pi-ai/dist/providers/azure-openai-responses.js
index b459932..d8e0832 100644
--- a/node_modules/@earendil-works/pi-ai/dist/providers/azure-openai-responses.js
+++ b/node_modules/@earendil-works/pi-ai/dist/providers/azure-openai-responses.js
@@ -181,6 +181,7 @@ function createClient(model, apiKey, options) {
     return new AzureOpenAI({
         apiKey,
         apiVersion,
+        fetch: options?.fetch,
         dangerouslyAllowBrowser: true,
         defaultHeaders: headers,
         baseURL: baseUrl,
diff --git a/node_modules/@earendil-works/pi-ai/dist/providers/openai-codex-responses.js b/node_modules/@earendil-works/pi-ai/dist/providers/openai-codex-responses.js
index 622375a..8459b58 100644
--- a/node_modules/@earendil-works/pi-ai/dist/providers/openai-codex-responses.js
+++ b/node_modules/@earendil-works/pi-ai/dist/providers/openai-codex-responses.js
@@ -145,12 +145,13 @@ export const streamOpenAICodexResponses = (model, context, options) => {
             // Fetch with retry logic for rate limits and transient errors
             let response;
             let lastError;
+            const fetchFn = options?.fetch ?? fetch;
             for (let attempt = 0; attempt <= MAX_RETRIES; attempt++) {
                 if (options?.signal?.aborted) {
                     throw new Error("Request was aborted");
                 }
                 try {
-                    response = await fetch(resolveCodexUrl(model.baseUrl), {
+                    response = await fetchFn(resolveCodexUrl(model.baseUrl), {
                         method: "POST",
                         headers: sseHeaders,
                         body: bodyJson,
diff --git a/node_modules/@earendil-works/pi-ai/dist/providers/openai-completions.js b/node_modules/@earendil-works/pi-ai/dist/providers/openai-completions.js
index 933e8a0..71b9a5b 100644
--- a/node_modules/@earendil-works/pi-ai/dist/providers/openai-completions.js
+++ b/node_modules/@earendil-works/pi-ai/dist/providers/openai-completions.js
@@ -74,7 +74,7 @@ export const streamOpenAICompletions = (model, context, options) => {
             const compat = getCompat(model);
             const cacheRetention = resolveCacheRetention(options?.cacheRetention);
             const cacheSessionId = cacheRetention === "none" ? undefined : options?.sessionId;
-            const client = createClient(model, context, apiKey, options?.headers, cacheSessionId, compat);
+            const client = createClient(model, context, apiKey, options?.headers, cacheSessionId, compat, options?.fetch);
             let params = buildParams(model, context, options, compat, cacheRetention);
             const nextParams = await options?.onPayload?.(params, model);
             if (nextParams !== undefined) {
@@ -349,7 +349,7 @@ export const streamSimpleOpenAICompletions = (model, context, options) => {
         toolChoice,
     });
 };
-function createClient(model, context, apiKey, optionsHeaders, sessionId, compat = getCompat(model)) {
+function createClient(model, context, apiKey, optionsHeaders, sessionId, compat = getCompat(model), fetch) {
     if (!apiKey) {
         if (!process.env.OPENAI_API_KEY) {
             throw new Error("OpenAI API key is required. Set OPENAI_API_KEY environment variable or pass it as an argument.");
@@ -384,6 +384,7 @@ function createClient(model, context, apiKey, optionsHeaders, sessionId, compat
     return new OpenAI({
         apiKey,
         baseURL: isCloudflareProvider(model.provider) ? resolveCloudflareBaseUrl(model) : model.baseUrl,
+        fetch,
         dangerouslyAllowBrowser: true,
         defaultHeaders,
     });
diff --git a/node_modules/@earendil-works/pi-ai/dist/providers/openai-responses.js b/node_modules/@earendil-works/pi-ai/dist/providers/openai-responses.js
index 396b4af..4ab1272 100644
--- a/node_modules/@earendil-works/pi-ai/dist/providers/openai-responses.js
+++ b/node_modules/@earendil-works/pi-ai/dist/providers/openai-responses.js
@@ -76,7 +76,7 @@ export const streamOpenAIResponses = (model, context, options) => {
             const apiKey = options?.apiKey || getEnvApiKey(model.provider) || "";
             const cacheRetention = resolveCacheRetention(options?.cacheRetention);
             const cacheSessionId = cacheRetention === "none" ? undefined : options?.sessionId;
-            const client = createClient(model, context, apiKey, options?.headers, cacheSessionId);
+            const client = createClient(model, context, apiKey, options?.headers, cacheSessionId, options?.fetch);
             let params = buildParams(model, context, options);
             const nextParams = await options?.onPayload?.(params, model);
             if (nextParams !== undefined) {
@@ -130,7 +130,7 @@ export const streamSimpleOpenAIResponses = (model, context, options) => {
         reasoningEffort,
     });
 };
-function createClient(model, context, apiKey, optionsHeaders, sessionId) {
+function createClient(model, context, apiKey, optionsHeaders, sessionId, fetch) {
     if (!apiKey) {
         if (!process.env.OPENAI_API_KEY) {
             throw new Error("OpenAI API key is required. Set OPENAI_API_KEY environment variable or pass it as an argument.");
@@ -167,6 +167,7 @@ function createClient(model, context, apiKey, optionsHeaders, sessionId) {
     return new OpenAI({
         apiKey,
         baseURL: isCloudflareProvider(model.provider) ? resolveCloudflareBaseUrl(model) : model.baseUrl,
+        fetch,
         dangerouslyAllowBrowser: true,
         defaultHeaders,
     });
diff --git a/node_modules/@earendil-works/pi-ai/dist/providers/simple-options.js b/node_modules/@earendil-works/pi-ai/dist/providers/simple-options.js
index a29c252..fba4ecf 100644
--- a/node_modules/@earendil-works/pi-ai/dist/providers/simple-options.js
+++ b/node_modules/@earendil-works/pi-ai/dist/providers/simple-options.js
@@ -3,6 +3,7 @@ export function buildBaseOptions(_model, options, apiKey) {
         temperature: options?.temperature,
         maxTokens: options?.maxTokens,
         signal: options?.signal,
+        fetch: options?.fetch,
         apiKey: apiKey || options?.apiKey,
         transport: options?.transport,
         cacheRetention: options?.cacheRetention,
diff --git a/node_modules/@earendil-works/pi-ai/dist/types.d.ts b/node_modules/@earendil-works/pi-ai/dist/types.d.ts
index 7958e7f..518de1c 100644
--- a/node_modules/@earendil-works/pi-ai/dist/types.d.ts
+++ b/node_modules/@earendil-works/pi-ai/dist/types.d.ts
@@ -29,6 +29,7 @@ export interface StreamOptions {
     temperature?: number;
     maxTokens?: number;
     signal?: AbortSignal;
+    fetch?: typeof globalThis.fetch;
     apiKey?: string;
     /**
      * Preferred transport for providers that support multiple transports.
