Agent: `Kant`

Independently compare `.context/external/pi-subagents` to Franklin's current capabilities in this repo. Do not read `.context/attachments` or `.context/algebra`. Use `packages/extensions`, `packages/mini-acp`, `packages/agent`, and Franklin built-ins as needed. Produce a memo with: (1) features that map directly, (2) features that need a shim, (3) features that require new Franklin APIs or protocol work, and (4) a phased path to a minimally useful transpiled version versus near-full parity. Cite concrete files. Keep it under 1200 words.

