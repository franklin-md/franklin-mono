Agent: `Epicurus`

Independently analyze fork/session/intercom behavior in `.context/external/pi-subagents`. Do not read `.context/attachments` or `.context/algebra`. Focus on `context=fresh` vs `fork`, `sessionManager` usage, session file assumptions, chat-back/intercom bridging, and any prompt/runtime contracts around inherited context. Use both source and tests. Produce a memo on what exact host/runtime support this requires and which semantics are fragile. Keep it under 1200 words.

