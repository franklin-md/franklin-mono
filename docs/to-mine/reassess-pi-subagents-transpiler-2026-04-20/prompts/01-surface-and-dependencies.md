Agent: `Newton`

Independently analyze the cloned repo at `.context/external/pi-subagents`. Do not read `.context/attachments` or `.context/algebra` and do not infer from prior summaries. Produce a source-based memo covering: (1) the public capability surface of pi-subagents, (2) every Pi extension API/event/UI/runtime capability it depends on, with concrete file references, and (3) which of those dependencies are core versus optional. Include counts where practical. Keep it under 1200 words.

