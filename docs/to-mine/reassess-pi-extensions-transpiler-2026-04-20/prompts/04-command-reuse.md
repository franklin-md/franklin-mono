Agent: `Banach`

Investigate whether Pi's command subsystem can be reused in Franklin. Do not read `.context/attachments` or `.context/algebra`. Inspect Pi files around slash commands, keybindings, flags, shortcuts, and extension registration, plus the Franklin extension registrar/compiler patterns in `packages/extensions`. Answer: (1) what code is pure registry/resolution vs TUI-coupled, (2) what interfaces a reusable core would need, (3) whether reuse is worth it versus a clean rebuild, and (4) a minimal Franklin `CommandAPI` shape. Cite concrete files. Keep it under 900 words.

