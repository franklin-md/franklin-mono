Agent: `Locke`

Investigate Pi's UI surface versus Franklin/mini-acp. Do not read `.context/attachments` or `.context/algebra`. Classify Pi `ctx.ui` methods into interaction, presentation, and TUI-only categories using source and examples. Then assess which interaction methods map naturally to protocol-level reverse RPC or extension APIs in Franklin/mini-acp, which require new primitives, and which are not realistic without a renderer. Cite concrete files and examples. Keep it under 900 words.

